# 002 Decorator Sample — デコレータの仕組み

`@app.route()` の仕組みを理解するために、Python デコレータを段階的に学ぶサンプルです。

## 目次

1. [基本の関数呼び出し](#1-基本の関数呼び出し) — 関数の定義と呼び出しを理解する
2. [関数を引数に渡す](#2-関数を引数に渡す) — 関数をオブジェクトとして扱う（sample2 / sample3）
3. [関数内関数（クロージャ）](#3-関数内関数クロージャ) — 関数の中に関数を定義する
4. [デコレータ構文（@）](#4-デコレータ構文) — `@` 構文でデコレータを適用する
5. [可変長引数付きデコレータ](#5-可変長引数付きデコレータ) — 任意の引数に対応したデコレータを作る
6. [練習問題](#6-練習問題)

---

## ファイル構成

```
002_decorator/
├── example/
│   ├── ex01.py   # 基本の関数呼び出し
│   ├── ex02.py   # 関数を引数に渡す
│   ├── ex03.py   # 関数は第一級オブジェクト（ポイント1〜3）
│   ├── ex04.py   # 関数内関数（クロージャ）
│   ├── ex05.py   # デコレータ構文（@）
│   └── ex06.py   # 可変長引数付きデコレータ
└── answer/
    ├── answer1.py
    └── answer2.py
```

---

## 1. 基本の関数呼び出し

> [example/ex01.py](example/ex01.py)

関数の定義と呼び出しの基本です。

```python
def a():
    print("A")

a()
```

### 用語：関数・第一級オブジェクト

| 用語 | 説明 |
|---|---|
| **関数** | 処理をひとまとめにして名前を付けたもの。`def` で定義する |
| **第一級オブジェクト** | 変数への代入・引数への受け渡し・戻り値として返すことができる値。Python の関数は第一級オブジェクト |

---

## 2. 関数を引数に渡す

> [example/ex02.py](example/ex02.py) | [example/ex03.py](example/ex03.py)

関数はオブジェクトであり、変数への代入や引数として渡せることを学びます。

**example/ex02.py** — 関数を引数として受け取り実行する

```python
def b(func):
    print("---Start---")
    func()          # 引数として受け取った関数を実行
    print("---End---")

b(a)
```

**example/ex03.py** — 関数は第一級オブジェクト（ポイント1〜3）

```python
# ポイント1：関数は変数に代入できる
x = a       # () を付けない → 関数オブジェクトを代入
print(x)    # <function a at 0x...>
x()         # A

# ポイント2：関数オブジェクトの情報を確認する
print(a)    # <function a at 0x...>

# ポイント3：print(a) と print(a()) の違い
print(a)    # 関数オブジェクトそのものを表示
print(a())  # a() を実行 → 戻り値 None を表示
```

| 書き方 | 動作 |
|---|---|
| `x = a` | 関数オブジェクトを変数に代入（実行しない） |
| `x = a()` | 関数を実行してその戻り値を代入 |
| `b(a)` | 関数オブジェクトを引数として渡す |
| `b(a())` | 関数を実行した戻り値を引数として渡す |

### 用語：高階関数

| 用語 | 説明 |
|---|---|
| **高階関数** | 関数を引数として受け取ったり、戻り値として返したりする関数 |

---

## 3. 関数内関数（クロージャ）

> [example/ex04.py](example/ex04.py)

関数の中に関数を定義し、それを戻り値として返すクロージャのパターンです。

### なぜ「関数を返す」必要があるのか

sample2 の `b(func)` は関数を受け取って**その場で実行**していました。これでは `b(a)` を呼んだ瞬間しか前後処理を挟めません。

```python
# sample2 の方法：呼び出すたびに b() を経由する必要がある
b(a)   # ---Start--- A ---End---
a()    # A（前後処理なし）
```

`a()` を呼ぶだけで常に前後処理が実行されるようにするには、`a` 自体を「前後処理付きの新しい関数」に置き換える必要があります。そのために「関数を返す」パターンを使います。

```python
def outer(func):
    def inner():
        print("---Start---")
        func()
        print("---End---")
    return inner         # inner 関数を返す（実行はしない）

result = outer(a)        # result = inner（前後処理付きの新しい関数）
result()                 # ---Start--- A ---End---
a = outer(a)             # a 自体を置き換えれば a() だけで前後処理が走る
```

### 用語：クロージャ

| 用語 | 説明 |
|---|---|
| **クロージャ** | 外側の関数の変数（ここでは `func`）を記憶しながら、内側の関数を戻り値として返すパターン |

`outer(a)` を呼ぶと `inner` 関数が返ります。`inner` は `func`（= `a`）を覚えたまま後から実行できます。これがデコレータの核心的な仕組みです。

---

## 4. デコレータ構文（@）

> [example/ex05.py](example/ex05.py)

クロージャを `@` 構文で簡潔に書いたものがデコレータです。`@app.route()` の仕組みと同じです。

```python
@outer
def a():
    print("A")

# 上記は以下と同等
# a = outer(a)
```

### 用語：デコレータ・シンタックスシュガー

| 用語 | 説明 |
|---|---|
| **デコレータ** | 関数に処理を追加・変更する仕組み。`@関数名` の形で適用する |
| **シンタックスシュガー** | 同じ動作をより簡潔に書けるようにした構文。`@outer` は `a = outer(a)` の糖衣構文 |

`@app.route("/")` も同じ仕組みで、Flask がビュー関数をラップしてルーティング情報を登録しています。

---

## 5. 可変長引数付きデコレータ

> [example/ex06.py](example/ex06.py)

`*args` / `**kwargs` を使い、任意の引数を持つ関数にも対応したデコレータです。

```python
def outer(func):
    def inner(*args, **kwargs):
        print("---Start---")
        func(*args, **kwargs)
        print("---End---")
    return inner
```

### 用語：可変長引数

| 引数 | 種類 | 説明 |
|---|---|---|
| `*args` | 位置引数の可変長 | 複数の値をタプルで受け取る |
| `**kwargs` | キーワード引数の可変長 | 複数のキーワード引数を辞書で受け取る |

`inner` が `*args` / `**kwargs` を受け取ることで、引数の数や種類を問わず任意の関数に適用できる汎用デコレータになります。

---

## 6. 練習問題

### 問題 1

以下のコードを完成させてください。`greet` 関数を実行したとき、出力結果が下記になるようにデコレータ `log` を作成してください。

```python
# ここにデコレータ log を定義する

@log
def greet():
    print("Hello!")
```

**期待する出力：**

```
=== 開始 ===
Hello!
=== 終了 ===
```

解答例 → [answer/answer1.py](answer/answer1.py)

---

### 問題 2

以下のコードを完成させてください。`add` 関数と `multiply` 関数の両方に適用できるデコレータ `show_result` を作成してください。

```python
# ここにデコレータ show_result を定義する

@show_result
def add(a, b):
    return a + b

@show_result
def multiply(a, b):
    return a * b
```

**期待する出力：**

```
add の結果 → 8
multiply の結果 → 15
```

解答例 → [answer/answer2.py](answer/answer2.py)
